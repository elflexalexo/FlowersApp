# NPM Configuration for FlowersApp
# Using legacy-peer-deps for React Native Expo compatibility
# This is a known issue in the Expo ecosystem with testing library versions
# Plan to upgrade after MVP release when newer Expo versions are stable

legacy-peer-deps=true
